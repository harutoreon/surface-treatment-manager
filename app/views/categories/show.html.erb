<%= render 'shared/header' %>

<div class="container w-25">
  <div class="fs-1 text-center mb-5">Category Show</div>

  <div class="text-center mb-3"><%= @category.item %></div>
  <div class="text-center"><%= link_to "Category Destroy", category_path(@category), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete?" } %></div>
</div>
