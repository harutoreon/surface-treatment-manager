<%= provide(:title, 'Category New') %>

<%= render 'shared/header' %>

<div class="container w-25">
  <div class="fs-1 text-center mb-5">Category New</div>

  <%= render 'form', category: @category %>

  <div class="text-center mt-5"><%= link_to 'Category Index', categories_path %></div>
</div>
